# DISABLED - Use deploy-frontend.yml for manual deployments
# This workflow was causing conflicts with Cloudflare Pages deployment

name: GitHub Pages (DISABLED)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Disabled
        run: |
          echo "This workflow has been disabled."
          echo "Use deploy-frontend.yml for manual Cloudflare Pages deployments."
          exit 1
