# DISABLED - Conflicts with Cloudflare Pages deployment
# To re-enable, remove the 'if: false' condition below

name: Deploy to GitHub Pages (DISABLED)

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    if: false  # DISABLED
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "This workflow is disabled"
